#pragma once
#include "gameobject.h"
#include <SFML/Graphics/Color.hpp>

class Border
{
	Border(float _posX, float _posY, float _width, float angle, float _height, const sf::Color color, int layer);
};

